# MongoDB connection string (use MongoDB Atlas for production)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/final-project?retryWrites=true&w=majority

# Port (Render sets this automatically)
PORT=3000
